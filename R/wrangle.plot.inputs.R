wrangle.model.plot.input_ <- function(cross,
                                      mean.formula,
                                      var.formula,
                                      group.by) {

  model.formulae

  model.df
}