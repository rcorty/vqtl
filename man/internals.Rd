% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula.utils.R, R/genetic.data.utils.R, R/scanonevar.R
\name{is.mean.formula}
\alias{additive.component}
\alias{dominance.component}
\alias{is.formulae}
\alias{is.mean.formula}
\alias{is.var.formula}
\alias{make.formulae}
\alias{remove.qtl.terms_}
\alias{replace.markers.with.add.dom_}
\alias{wrangle.genoprob.df_}
\alias{wrangle.loc.info.df_}
\alias{wrangle.scanonevar.input_}
\alias{wrangle.scanonevar.types_}
\title{is.mean.formula}
\usage{
is.mean.formula(x)

is.var.formula(x)

is.formulae(x)

make.formulae(mean.formula, var.formula)

replace.markers.with.add.dom_(cross, mean.formula, var.formula)

remove.qtl.terms_(formulae)

wrangle.loc.info.df_(cross, chrs)

wrangle.genoprob.df_(cross)

additive.component(genoprobs.long)

dominance.component(genoprobs.long)

wrangle.scanonevar.input_(cross, mean.formula, var.formula, chrs)

wrangle.scanonevar.types_(mean.formula, var.formula)
}
\arguments{
\item{x}{object to be tested whether it is a valid mean.formula}

\item{mean.formula}{a mean.formula}

\item{var.formula}{The formula to describe the residual variance of the
phenotype.  Keywords are var.QTL.add and var.QTL.dom for the additive and
dominance components of the QTL effect on residual phenotype variance.}

\item{cross}{The \code{cross}, built by \pkg{qtl} to be used in mapping}

\item{formulae}{The formulae from which QTL terms will be removed}

\item{genoprobs.long}{The genoprobs from which the additive genetic
component should be extracted.  The 'long' format implies that each row
has information on the genoprob of one individual having one allele.}

\item{x}{object to be tested whether it is a valid var.formula}

\item{x}{object to be tested whether it is a valid formulae}

\item{var.forula}{a var.formula}

\item{genoprobs.long}{The genoprobs from which the additive genetic
component should be extracted.  The 'long' format implies that each row
has information on the genoprob of one individual having one allele.}
}
\value{
TRUE if x is a valid mean.formula, FALSE otherwise

TRUE if X is a valid var.formula, FALSE otherwise

TRUE if x is a valid formulae, FALSE otherwise

a formulae, a list of length two where the first element is named
'mean.formula' and is a mean.formula and the second element is named
'var.formula' and is a var.formula.

A a list with two elements, mean.formula and var.formula,
where any markers from \code{cross} in the input mean.formula or var.formula
have been replaced with (marker.name_add + marker.name_dom).

a formulae object similar to the input formulae, but with QTL terms removed

A vector of the additive genetic component at the locus

A vector of the dominance genetic component at the locus

a list with three elements: 'model.df', the data.frame to be used
at every genetic locus in the upcoming scanonevar_, 'scan.df', the
data.frame that will be picked from, one genetic at a time to fill the
'QTL' variable in the model and 'scan.types', which indicates which types
of scan to do: mqtl, vqtl, and/or mvqtl.

either 'mean', 'var', or c('mean', 'var', 'joint'), indicating which
types of scans are implied by mean.formula and var.formula
}
\examples{
x <- 27599

}

